package jetbrains.mps.plugin;

/*Generated by MPS */

import com.intellij.openapi.components.ApplicationComponent;
import jetbrains.mps.ide.editor.actions.GoToDeclarationHandler;
import jetbrains.mps.project.MPSProject;
import org.jetbrains.mps.openapi.model.SNode;
import jetbrains.mps.ide.navigation.NavigationProvider;
import org.jetbrains.annotations.NonNls;
import org.jetbrains.annotations.NotNull;

public class IdeaGoToDeclarationHandlerInitializer implements ApplicationComponent {
  private GoToDeclarationHandler myHandler = new GoToDeclarationHandler() {
    public boolean canNavigate(MPSProject p, SNode node) {
      for (NavigationProvider np : IdeaNavigationProvider.EP_NAME.getExtensions()) {
        if (np.canNavigate(p, node)) {
          return true;
        }
      }
      return false;
    }
    public void navigate(MPSProject p, SNode node) {
      for (NavigationProvider np : IdeaNavigationProvider.EP_NAME.getExtensions()) {
        if (np.canNavigate(p, node)) {
          np.navigate(p, node);
        }
      }
    }
  };
  @Override
  public void initComponent() {
    GoToDeclarationHandler.addHandler(this.myHandler);
  }
  @Override
  public void disposeComponent() {
    GoToDeclarationHandler.removeHandler(this.myHandler);
  }
  @NonNls
  @NotNull
  @Override
  public String getComponentName() {
    return getClass().getName();
  }
}
